import React from 'react';
import { SpinnerContainer } from '../styles';

/**
 * Spinner functional component
 * 
 * @param {*} props 
 */
const SpinnerFC = (props) => (
    <SpinnerContainer>
        Loading...
    </SpinnerContainer>
);

export default SpinnerFC;